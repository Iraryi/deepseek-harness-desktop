/** Host loader entry for the DSH HUB browser implementation exported from `./client`. */

/** Host plugin body — Setup catalog behavior runs only in the browser bundle. */
export function apply(): void {}
