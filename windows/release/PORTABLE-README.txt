DeepSeek Harness Portable

Run dsh.exe. The first launch opens CONFIG before the main application.
All settings and user data stay in the data directory beside the executable.
Microsoft Edge WebView2 Runtime must already be available on the computer.

运行 dsh.exe。首次启动会先打开 CONFIG，然后进入主程序。
所有设置和用户数据都保存在程序旁边的 data 文件夹中。
电脑必须已经具备 Microsoft Edge WebView2 Runtime。
